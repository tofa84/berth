# Xcode — user-specific (leaks username, holds local UI/scheme state)
xcuserdata/
*.xcuserstate

# Build output
DerivedData/
.build/
build/
*.xcframework

# SwiftPM
.swiftpm/

# macOS
.DS_Store

# Release output (built locally by scripts/release.sh)
dist/

# Local agent notes (not part of the project)
CLAUDE.md
